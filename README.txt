This demo reads the file C:/Users/Public/Documents/GelSight/Scans/japanese-coin.tmd.
Place the file in that location or change the path.

The property sheets use the environment variable $(GS_THIRDPARTYDIR) as the toplevel directory containing openCV.

Change the property sheets or set the environment variable to match your installation.